<nav class="nav">
  <a href="/" class="brand">JobFinder</a>
  <div class="nav-links">
    <% if (user) { %>
      <span class="nav-user"><%= user.email %><% if (user.role === 'jobseeker') { %> (Job seeker)<% } else { %> (Employer)<% } %></span>
      <a href="<%= user.role === 'jobseeker' ? '/jobs' : '/jobs/my' %>">Dashboard</a>
      <a href="/profiles/edit">Profile</a>
      <form action="/logout" method="post" style="display:inline">
        <button type="submit" class="btn-link">Log out</button>
      </form>
    <% } else { %>
      <a href="/login">Log in</a>
      <a href="/register">Register</a>
    <% } %>
  </div>
</nav>
